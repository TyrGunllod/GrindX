---
phase: 03-performance-resilience
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/schemas/base.py
  - apps/api-postgres/app/routers/health_router.py
  - apps/api-sqlserver/app/routers/health_router.py
  - apps/api-postgres/tests/integration/test_health.py
  - apps/api-sqlserver/tests/integration/test_health.py
autonomous: true
requirements:
  - PERF-03

must_haves:
  truths:
    - "Health endpoint returns HTTP 200 with status 'healthy' when DB is reachable"
    - "Health endpoint returns HTTP 503 with status 'degraded' when DB is unreachable"
    - "Health check verifies database connectivity and schema integrity"
    - "Both api-postgres and api-sqlserver have deep health checks"
  artifacts:
    - path: "apps/api-postgres/app/routers/health_router.py"
      provides: "Enhanced health check with schema validation"
      contains: "check_database_health"
    - path: "apps/api-sqlserver/app/routers/health_router.py"
      provides: "Enhanced health check with schema validation"
      contains: "check_database_health"
    - path: "apps/api-postgres/tests/integration/test_health.py"
      provides: "Health check tests (healthy + degraded scenarios)"
      contains: "test_"
    - path: "apps/api-sqlserver/tests/integration/test_health.py"
      provides: "Health check tests (healthy + degraded scenarios)"
      contains: "test_"
  key_links:
    - from: "apps/api-postgres/app/routers/health_router.py"
      to: "packages/shared/schemas/base.py"
      via: "HealthCheckResponse schema"
      pattern: "HealthCheckResponse"
    - from: "apps/api-sqlserver/app/routers/health_router.py"
      to: "packages/shared/schemas/base.py"
      via: "HealthCheckResponse schema"
      pattern: "HealthCheckResponse"
---

<objective>
Enhance health check endpoints for both APIs with deep database connectivity and schema validation.

Purpose: Provide accurate health status for container orchestration (Kubernetes, Docker) — HTTP 200 when healthy, HTTP 503 with details when degraded.

Output: Updated HealthCheckResponse schema, enhanced health routers for both APIs, integration tests for healthy and degraded scenarios.
</objective>

<execution_context>
@C:/Users/alexb/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/alexb/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-performance-resilience/03-CONTEXT.md
@.planning/phases/03-performance-resilience/03-RESEARCH.md
@packages/shared/schemas/base.py
@apps/api-postgres/app/routers/health_router.py
@apps/api-sqlserver/app/routers/health_router.py
@apps/api-postgres/tests/conftest.py
@apps/api-postgres/app/core/config.py
@apps/api-postgres/app/modules/org/models/empresa.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update HealthCheckResponse schema and create health check logic</name>
  <files>packages/shared/schemas/base.py, apps/api-postgres/app/routers/health_router.py, apps/api-sqlserver/app/routers/health_router.py</files>
  <read_first>
    - packages/shared/schemas/base.py (current HealthCheckResponse schema)
    - apps/api-postgres/app/routers/health_router.py (current postgres health check)
    - apps/api-sqlserver/app/routers/health_router.py (current sqlserver health check)
    - apps/api-postgres/app/core/config.py (APP_NAME, APP_VERSION)
    - apps/api-postgres/app/modules/org/models/empresa.py (Empresa model for table check)
    - apps/api-postgres/app/modules/iam/models/usuario.py (Usuario model for table check)
    - apps/api-postgres/app/modules/portal/models/portal.py (Aba model for table check)
  </read_first>
  <behavior>
    - Test: Health endpoint returns 200 when database is reachable
    - Test: Health endpoint returns 503 when database is unreachable
    - Test: Health response includes database details (postgres status)
    - Test: Health response includes schema validation (missing tables detected)
    - Test: Health response includes service name and version
    - Test: Health response includes timestamp
  </behavior>
  <action>
    1. Modify `packages/shared/schemas/base.py` — update `HealthCheckResponse`:
       - Change `database` field from `str` to `dict[str, Any]` with description "Status detalhado da conexão com o banco de dados"
       - Add `details: dict[str, Any] | None = Field(default=None, description="Detalhes adicionais quando status é degraded")` field
       - Keep `status`, `service`, `version`, `timestamp` fields unchanged
       - Update `json_schema_extra` examples to show new structure:
         ```python
         json_schema_extra={
             "examples": [{
                 "status": "healthy",
                 "service": "api-postgres",
                 "version": "0.1.0",
                 "database": {"postgres": "connected"},
                 "timestamp": "2026-06-06T12:00:00Z",
                 "details": None
             }]
         }
         ```

    2. Modify `apps/api-postgres/app/routers/health_router.py`:
       - Import `inspect` from `sqlalchemy`
       - Add function `check_database_health(db: Session) -> dict` that:
         - Executes `SELECT 1` to verify connectivity
         - Uses `inspect(db.bind)` to get table names
         - Checks for critical tables: `usuarios`, `company_themes`, `portal_abas`, `empresas`
         - Returns `{"status": "connected", "missing_tables": []}` on success
         - Returns `{"status": "disconnected", "error": str(exc)}` on failure
         - Returns `{"status": "degraded", "missing_tables": [...]}` if tables missing
       - Update `health_check` endpoint:
         - Call `check_database_health(db)` to get detailed status
         - If `db_result["status"] == "connected"` and no missing tables: return HTTP 200 with `{"status": "healthy", "database": {"postgres": "connected"}, ...}`
         - If `db_result["status"] == "disconnected"` or missing tables: return HTTP 503 with `{"status": "degraded", "database": {"postgres": db_result["status"]}, "details": {"missing_tables": db_result.get("missing_tables", []), "error": db_result.get("error")}}`
         - Use `JSONResponse` from starlette for 503 responses
       - Log health check events with structlog (already imported)

    3. Modify `apps/api-sqlserver/app/routers/health_router.py`:
       - Same pattern as api-postgres but:
         - Check for SQL Server specific tables (query `INFORMATION_SCHEMA.TABLES` or use `inspect`)
         - Use `{"sqlserver": "connected"}` in database dict
         - Critical tables for SQL Server: determine from existing queries in the API (check routers for table references)
         - If tables cannot be determined, skip schema validation and only check connectivity
       - Return same response format as api-postgres

    4. Update all existing tests that depend on `HealthCheckResponse.database` being a string:
       - Search for tests using `health_check` or `HealthCheckResponse` and update assertions
       - The `database` field is now a dict, not a string
  </action>
  <verify>
    <automated>cd apps/api-postgres && PYTHONPATH=../../packages python -m pytest tests/integration/test_health.py -x -v 2>/dev/null; cd ../.. && PYTHONPATH=packages python -c "from shared.schemas.base import HealthCheckResponse; print(HealthCheckResponse.model_fields.keys())"</automated>
  </verify>
  <done>
    - HealthCheckResponse.database is now dict[str, Any] with per-DB status
    - HealthCheckResponse.details field added for degraded scenarios
    - api-postgres health check verifies connectivity AND schema (4 critical tables)
    - api-sqlserver health check verifies connectivity
    - HTTP 200 when healthy, HTTP 503 when degraded
    - Structured logging for health check events
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests for health check scenarios</name>
  <files>apps/api-postgres/tests/integration/test_health.py, apps/api-sqlserver/tests/integration/test_health.py</files>
  <read_first>
    - apps/api-postgres/tests/conftest.py (test fixtures, client, db_session)
    - apps/api-postgres/app/routers/health_router.py (health check endpoint to test)
    - apps/api-sqlserver/app/routers/health_router.py (health check endpoint to test)
    - apps/api-postgres/app/core/config.py (APP_NAME, APP_VERSION)
  </read_first>
  <behavior>
    - Test: GET /health returns 200 when database is reachable (db_session fixture)
    - Test: GET /health response has status "healthy" when database is reachable
    - Test: GET /health response includes "database" dict with "postgres" key
    - Test: GET /health response includes service name and version
    - Test: GET /health response includes timestamp
    - Test: GET /health returns 503 when database connection fails (mock exception)
    - Test: GET /health response has status "degraded" when database fails
    - Test: GET /health response includes error details when degraded
  </behavior>
  <action>
    1. Create `apps/api-postgres/tests/integration/test_health.py`:
       - Import `pytest`, `unittest.mock.patch`, `TestClient` patterns from conftest
       - Test `test_health_check_healthy`:
         - Use `client` fixture (which has db_session override)
         - `response = client.get("/health")`
         - Assert `response.status_code == 200`
         - Assert `response.json()["status"] == "healthy"`
         - Assert `"postgres" in response.json()["database"]`
         - Assert `response.json()["database"]["postgres"] == "connected"`
         - Assert `response.json()["service"]` is not None
         - Assert `response.json()["version"]` is not None
         - Assert `response.json()["timestamp"]` is not None
       - Test `test_health_check_degraded_on_db_failure`:
         - Patch `sqlalchemy.orm.Session.execute` to raise `Exception("Connection refused")`
         - `response = client.get("/health")`
         - Assert `response.status_code == 503`
         - Assert `response.json()["status"] == "degraded"`
         - Assert `"postgres" in response.json()["database"]`
         - Assert `response.json()["database"]["postgres"] == "disconnected"`
         - Assert `response.json()["details"]` is not None
         - Assert `"error" in response.json()["details"]`
       - Use `@pytest.mark.integration` marker

    2. Create `apps/api-sqlserver/tests/integration/test_health.py`:
       - Same pattern as api-postgres but:
         - Check for `"sqlserver"` key in database dict
         - Use api-sqlserver's test fixtures (check `apps/api-sqlserver/tests/conftest.py`)
         - If api-sqlserver doesn't have conftest.py, create minimal fixtures
       - Test `test_health_check_healthy`: 200, status "healthy", "sqlserver" in database dict
       - Test `test_health_check_degraded_on_db_failure`: 503, status "degraded", error details

    3. Ensure both test files use `PYTHONPATH` correctly:
       - api-postgres tests: `PYTHONPATH=../../packages`
       - api-sqlserver tests: `PYTHONPATH=../../packages`
  </action>
  <verify>
    <automated>cd apps/api-postgres && PYTHONPATH=../../packages python -m pytest tests/integration/test_health.py -x -v</automated>
  </verify>
  <done>
    - apps/api-postgres/tests/integration/test_health.py exists and passes
    - apps/api-sqlserver/tests/integration/test_health.py exists and passes
    - Tests cover healthy scenario (200, status "healthy", database dict)
    - Tests cover degraded scenario (503, status "degraded", error details)
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external→health_endpoint | Health endpoint is public (no auth), exposes service status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-03-05 | Information Disclosure | Health endpoint | accept | Health endpoint exposes service status (healthy/degraded) which is standard for container orchestration. No sensitive data (secrets, user data) is exposed. |
| T-03-06 | Denial of Service | Health endpoint | accept | Health check runs SELECT 1 + inspect(), which are lightweight. No expensive operations. |
| T-03-SC | Tampering | Schema changes | mitigate | Schema validation checks critical tables, catches migration issues early |
</threat_model>

<verification>
- `cd apps/api-postgres && PYTHONPATH=../../packages python -m pytest tests/integration/test_health.py -x -v` — all tests pass
- `cd apps/api-sqlserver && PYTHONPATH=../../packages python -m pytest tests/integration/test_health.py -x -v` — all tests pass
- `curl http://localhost:8002/health` — returns 200 with `{"status": "healthy", "database": {"postgres": "connected"}, ...}`
- `PYTHONPATH=packages python -c "from shared.schemas.base import HealthCheckResponse; m = HealthCheckResponse(status='healthy', service='test', version='1.0', database={'postgres': 'connected'}); print(m.model_dump())"` — schema works
- `grep "503" apps/api-postgres/app/routers/health_router.py` — HTTP 503 for degraded
- `grep "check_database_health" apps/api-postgres/app/routers/health_router.py` — function exists
</verification>

<success_criteria>
- HealthCheckResponse schema updated with dict database field and details field
- api-postgres health check returns 200 when DB reachable, 503 when unreachable
- api-sqlserver health check returns 200 when DB reachable, 503 when unreachable
- Health checks verify schema integrity (critical tables exist)
- Both APIs have integration tests for healthy and degraded scenarios
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/03-performance-resilience/03-03-SUMMARY.md` when done
</output>
