---
phase: 02-infrastructure-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api-postgres/alembic/versions/001_create_tables.py
  - apps/api-postgres/alembic/versions/0001_criar_tabela_projetos.py
  - apps/api-postgres/alembic/versions/02_add_org_schema_tables.py
  - apps/api-postgres/alembic/versions/__pycache__
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "alembic heads retorna exatamente um head (sem conflitos de migração)"
    - "Tabelas do schema org (projetos, recursos, tarefas, registros_tarefas) existem na cadeia canônica"
    - "Migrações órfãs 001_create_tables.py e 0001_criar_tabela_projetos.py foram removidas"
  artifacts:
    - path: "apps/api-postgres/alembic/versions/02_add_org_schema_tables.py"
      provides: "Migração consolidada para tabelas do schema org"
      contains: "revision"
    - path: "apps/api-postgres/alembic/versions/001_create_tables.py"
      provides: "DEVE SER REMOVIDO (migração órfã)"
      must_not_exist: true
    - path: "apps/api-postgres/alembic/versions/0001_criar_tabela_projetos.py"
      provides: "DEVE SER REMOVIDO (migração órfã)"
      must_not_exist: true
  key_links:
    - from: "02_add_org_schema_tables.py"
      to: "2026_06_02_add_temp_password_fields.py"
      via: "down_revision = 'a1b2c3d4e5f6'"
      pattern: "down_revision.*a1b2c3d4e5f6"
---

<objective>
Consolidar migrações Alembic duplicadas que produzem múltiplos heads.

Purpose: Resolver conflitos de prefixo `001_*` em alembic/versions/ — existem 3 cadeias independentes com `down_revision = None`. As migrações órfãs (`001_create_tables.py` e `0001_criar_tabela_projetos.py`) criam tabelas do schema `org` que sobrepõem a cadeia canônica. A consolidação remove os arquivos órfãs e cria uma nova migração no head da cadeia canônica para as tabelas `org` (projetos, recursos, tarefas, registros_tarefas).

Output: Cadeia de migração única e linear, `alembic heads` retorna exatamente 1 head.
</objective>

<execution_context>
@C:/Users/alexb/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/alexb/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-infrastructure-quality/02-RESEARCH.md
@apps/api-postgres/alembic/versions/001_initial_schema.py
@apps/api-postgres/alembic/versions/001_create_tables.py
@apps/api-postgres/alembic/versions/0001_criar_tabela_projetos.py
@apps/api-postgres/alembic/versions/2026_06_02_add_temp_password_fields.py
@apps/api-postgres/alembic/env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar migrações órfãs e criar migração consolidada para schema org</name>
  <files>
    apps/api-postgres/alembic/versions/001_create_tables.py,
    apps/api-postgres/alembic/versions/0001_criar_tabela_projetos.py,
    apps/api-postgres/alembic/versions/02_add_org_schema_tables.py,
    apps/api-postgres/alembic/versions/__pycache__
  </files>
  <read_first>
    apps/api-postgres/alembic/versions/001_create_tables.py,
    apps/api-postgres/alembic/versions/0001_criar_tabela_projetos.py,
    apps/api-postgres/alembic/versions/2026_06_02_add_temp_password_fields.py,
    apps/api-postgres/alembic/env.py
  </read_first>
  <action>
    1. Deletar `apps/api-postgres/alembic/versions/001_create_tables.py` (migração órfã — revision "001", down_revision=None, cria org.projetos + org.recursos + org.tarefas + org.registros_tarefas)
    2. Deletar `apps/api-postgres/alembic/versions/0001_criar_tabela_projetos.py` (migração órfã — revision "0001", down_revision=None, cria org.projetos apenas)
    3. Deletar o diretório `apps/api-postgres/alembic/versions/__pycache__/` se existir (limpar bytecode das migrações deletadas)
    4. Criar nova migração `apps/api-postgres/alembic/versions/02_add_org_schema_tables.py` com:
       - `revision = "b2c3d4e5f6a7"`
       - `down_revision = "a1b2c3d4e5f6"` (head atual da cadeia canônica — migração temp_password)
       - Função `upgrade()` que cria as 4 tabelas do schema `org` usando o código exato de `001_create_tables.py` (projetos, recursos, tarefas, registros_tarefas com todas as colunas, foreign keys, índices e constraints). Copiar a estrutura completa de `001_create_tables.py` — não simplificar.
       - Função `downgrade()` que dropa as 4 tabelas na ordem reversa (registros_tarefas → tarefas → recursos → projetos)
       - Comentário no topo: "Consolidate org schema tables — replaces orphan migrations 001_create_tables and 0001_criar_tabela_projetos"
       - Incluir `CREATE SCHEMA IF NOT EXISTS org` antes de `create_table` para garantir que o schema existe
    5. Verificar que a tabela `projetos` usa `sa.ForeignKeyConstraint(["gerente_id"], ["iam.usuarios.id"], ondelete="SET NULL")` — copiar de `001_create_tables.py` que já tem essa constraint correta
    6. Verificar que os índices estão corretos: `ix_org_projetos_nome`, `ix_org_tarefas_titulo`, `ix_org_tarefas_projeto_id`, `ix_org_tarefas_responsavel_id`, `ix_org_registros_tarefas_tarefa_id`, `ix_org_registros_tarefas_autor_id`
  </action>
  <verify>
    <automated>cd apps/api-postgres && python -m alembic heads</automated>
    <automated>cd apps/api-postgres && python -m alembic history</automated>
    <automated>cd apps/api-postgres && python -m alembic check</automated>
  </verify>
  <acceptance_criteria>
    - `alembic heads` retorna exatamente 1 head: "b2c3d4e5f6a7 (head)"
    - `alembic history` mostra cadeia linear: b2c3d4e5f6a7 → a1b2c3d4e5f6 → 005_add_aba_parent_id → 004_add_theme_history → 003_add_empresa_and_theme → 002_add_usuario_modulos → 001_initial_schema
    - Arquivo `001_create_tables.py` NÃO existe mais em `apps/api-postgres/alembic/versions/`
    - Arquivo `0001_criar_tabela_projetos.py` NÃO existe mais em `apps/api-postgres/alembic/versions/`
    - Arquivo `02_add_org_schema_tables.py` existe com `revision = "b2c3d4e5f6a7"` e `down_revision = "a1b2c3d4e5f6"`
    - Nova migração cria tabelas: org.projetos, org.recursos, org.tarefas, org.registros_tarefas
    - `alembic check` não reporta diferenças pendentes (ou reporta apenas as novas tabelas org se o banco não as tem)
  </acceptance_criteria>
  <done>
    Cadeia de migração Alembic é linear com um único head. Migrações órfãs removidas. Tabelas do schema org consolidadas na cadeia canônica.
  </done>
</task>

</tasks>

<verification>
Verificação final da consolidação:
1. `cd apps/api-postgres && python -m alembic heads` → exatamente 1 head
2. `cd apps/api-postgres && python -m alembic history` → cadeia linear sem branches
3. `ls apps/api-postgres/alembic/versions/` → não contém `001_create_tables.py` nem `0001_criar_tabela_projetos.py`
4. `grep -r "down_revision = None" apps/api-postgres/alembic/versions/` → apenas `001_initial_schema.py`
</verification>

<success_criteria>
- `alembic heads` retorna exatamente 1 head (b2c3d4e5f6a7)
- Migrações órfãs deletadas (001_create_tables.py, 0001_criar_tabela_projetos.py)
- Nova migração consolida tabelas org na cadeia canônica
- Cadeia de migração é linear sem branches
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure-quality/02-01-SUMMARY.md` when done
</output>
