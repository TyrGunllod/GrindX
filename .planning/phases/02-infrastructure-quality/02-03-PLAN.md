---
phase: 02-infrastructure-quality
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - apps/api-postgres/tests/unit/test_schema_validation.py
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "Teste de validação confirma que _SCHEMA_TRANSLATE cobre todos os schemas PostgreSQL (iam, portal, catalogo, org)"
    - "Teste confirma que todos os schemas mapeiam para None (compatível com SQLite)"
    - "Teste passa com a configuração atual de _SCHEMA_TRANSLATE em conftest.py"
  artifacts:
    - path: "apps/api-postgres/tests/unit/test_schema_validation.py"
      provides: "Testes de validação do schema translate map"
      contains: "test_schema_translate"
  key_links:
    - from: "apps/api-postgres/tests/unit/test_schema_validation.py"
      to: "apps/api-postgres/tests/conftest.py"
      via: "import _SCHEMA_TRANSLATE"
      pattern: "from.*conftest.*import.*_SCHEMA_TRANSLATE"
---

<objective>
Criar teste de validação que garante que `_SCHEMA_TRANSLATE` em conftest.py cobre todos os schemas PostgreSQL usados pela aplicação.

Purpose: INFRA-03 exige que exista um teste automatizado verificando que o dicionário `_SCHEMA_TRANSLATE` mapeia todos os schemas PostgreSQL (`iam`, `portal`, `catalogo`, `org`) para `None` (compatível com SQLite). Atualmente não existe esse teste — se alguém adicionar um novo schema PostgreSQL e esquecer de atualizar `_SCHEMA_TRANSLATE`, os testes passam em SQLite mas a aplicação falha em produção. Este teste previne essa classe de bugs.

Output: Teste unitário que valida _SCHEMA_TRANSLATE contra lista canônica de schemas.
</objective>

<execution_context>
@C:/Users/alexb/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/alexb/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-infrastructure-quality/02-RESEARCH.md
@apps/api-postgres/tests/conftest.py
@apps/api-postgres/tests/unit/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar teste de validação do schema translate map</name>
  <files>
    apps/api-postgres/tests/unit/test_schema_validation.py
  </files>
  <read_first>
    apps/api-postgres/tests/conftest.py,
    .planning/phases/02-infrastructure-quality/02-RESEARCH.md,
    apps/api-postgres/tests/unit/test_auth.py
  </read_first>
  <action>
    1. Criar arquivo `apps/api-postgres/tests/unit/test_schema_validation.py` com os seguintes testes:

    2. Definir constante `EXPECTED_SCHEMAS = {"iam", "portal", "catalogo", "org"}` no topo do arquivo — esta é a lista canônica de schemas PostgreSQL usados pela aplicação.

    3. Criar teste `test_schema_translate_covers_all_schemas()` que:
       - Importa `_SCHEMA_TRANSLATE` de `apps.api_postgres.tests.conftest` (ou usar import relativo se conftest estiver no mesmo package)
       - Verifica que cada schema em `EXPECTED_SCHEMAS` está presente como chave em `_SCHEMA_TRANSLATE`
       - Mensagem de erro em português: "Schema '{schema}' não está em _SCHEMA_TRANSLATE. Adicione '{schema}: None' ao dicionário em conftest.py."

    4. Criar teste `test_schema_translate_maps_to_none_for_sqlite()` que:
       - Itera sobre todos os pares (schema, target) em `_SCHEMA_TRANSLATE`
       - Verifica que `target is None` para cada um
       - Mensagem de erro em português: "Schema '{schema}' mapeia para '{target}' em vez de None. Para testes com SQLite, todos os schemas devem mapear para None."

    5. Criar teste `test_expected_schemas_match_application_models()` que:
       - Verifica que `EXPECTED_SCHEMAS` contém exatamente 4 schemas (não mais, não menos)
       - Usa `assert len(EXPECTED_SCHEMAS) == 4` como sanity check
       - Mensagem de erro: "Número inesperado de schemas. Atualize EXPECTED_SCHEMAS se novos schemas foram adicionados."

    6. Seguir padrão de naming do projeto: testes em português, nomes descritivos com `test_` prefix.

    7. NÃO adicionar marker `@pytest.mark.integration` — estes são testes unitários rápidos.
  </action>
  <verify>
    <automated>cd apps/api-postgres && set PYTHONPATH=..\..\packages && python -m pytest tests/unit/test_schema_validation.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/api-postgres/tests/unit/test_schema_validation.py` existe
    - Contém 3 testes: `test_schema_translate_covers_all_schemas`, `test_schema_translate_maps_to_none_for_sqlite`, `test_expected_schemas_match_application_models`
    - Todos os 3 testes passam
    - `_SCHEMA_TRANSLATE` é importado de `apps.api_postgres.tests.conftest` (ou caminho equivalente que funcione)
    - Mensagens de erro estão em português
  </acceptance_criteria>
  <done>
    Testes de validação do schema translate map criados e passando. Todos os 4 schemas PostgreSQL (iam, portal, catalogo, org) são validados contra _SCHEMA_TRANSLATE.
  </done>
</task>

</tasks>

<verification>
Verificação final do teste de schema validation:
1. `cd apps/api-postgres && python -m pytest tests/unit/test_schema_validation.py -v` → 3 testes passam
2. `grep "EXPECTED_SCHEMAS" apps/api-postgres/tests/unit/test_schema_validation.py` → {"iam", "portal", "catalogo", "org"}
3. `grep "_SCHEMA_TRANSLATE" apps/api-postgres/tests/conftest.py` → {"iam": None, "portal": None, "catalogo": None, "org": None}
</verification>

<success_criteria>
- 3 testes criados em test_schema_validation.py
- Todos passam com _SCHEMA_TRANSLATE atual
- Testes validam cobertura completa dos 4 schemas PostgreSQL
- Mensagens de erro em português
</success_criteria>

<output>
Create `.planning/phases/02-infrastructure-quality/02-03-SUMMARY.md` when done
</output>
